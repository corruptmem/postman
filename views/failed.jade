h1 Failed Emails
table
  thead
    tr
      th Date
      th Subject
      th To
      th From
      th 
  tbody
    each failedMail in failed
      tr
        td= failedMail.date.toShortDateTimeString()
        td= failedMail.mail.subject
        td= failedMail.mail.to
        td= failedMail.mail.from
        td
          a(href="/failed/" + failedMail.id) View
